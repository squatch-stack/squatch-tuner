// SPDX-License-Identifier: MIT
// Latest<T> moved into the core (squatch/dsp/latest.hpp) so blocks can publish through it too.
#pragma once
#include <squatch/dsp/latest.hpp>

namespace squatch::host {
using squatch::dsp::Latest;
}  // namespace squatch::host
